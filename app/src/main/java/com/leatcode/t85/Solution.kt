package com.leatcode.t85

import java.util.TreeSet

class Solution {
    fun maximalRectangle(matrix: Array<CharArray>): Int {
//        var max = 0
//        val onePrefixes = Array(matrix.size) { IntArray(matrix[it].size) }
//
//        for (i in matrix.indices) {
//            for (j in matrix[i].indices) {
//                if (matrix[i][j] == '0') continue
//                onePrefixes[i][j] = (onePrefixes.getOrNull(i - 1)?.get(j) ?: 0) + 1
//            }
//
//            val maxSet = TreeSet<Int>()
//            for (j1 in matrix[i].indices) {
//                for (j2 in 0)
//            }
//        }

        TODO()

    }
}