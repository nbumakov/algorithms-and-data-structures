package com.leetcode.t2516

class Solution {
    private val letters = setOf('a', 'b', 'c')

    // aabaaaacaabcaabaaaacaabc
    fun takeCharacters(s: String, k: Int): Int {
//        if (letters.any { letter -> s.count { it == letter} < k }) return -1
//        val ss = s + s
//        val counter = s.groupBy { it }.map { it.key to it.value.size }.toMap().toMutableMap()
//        if (aCount < k || bCount < k || cCount < k) return -1
//
//        var l = 0
//        var r = ss.lastIndex
        TODO()
    }

}